# Agent guide

Route exposure authoring through `sim-estate-project`, then prove provider lifecycle behavior with `sim-site-estate-model`. Never add native execution vocabulary to caller-facing records.

