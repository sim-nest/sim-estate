schema = "sim.features"

[[feature]]
id = "feature/sim-estate/estate-vocabulary"
title = "Portable estate operation vocabulary"
summary = "Versioned, bounded records and a provider contract cover estate planning through reconciliation without native identities."
owner = "crate/sim-estate-core"
audiences = ["code", "framework"]
guidance = "Use EstateProvider with typed targets and sanitized inventory. Preserve Plan fingerprints through approval and reconcile every post-dispatch ambiguity; provider-private text never enters these records."
claims_anchors = ["anchor/crate/sim-estate-core"]

[[feature]]
id = "feature/sim-estate/exposure-authoring"
title = "Closed estate exposure authoring"
summary = "Compile strict declarations into operations whose callers can supply only shaped parameter values."
owner = "crate/sim-estate-project"
audiences = ["code", "framework"]
guidance = "Declare literal program references, whole argument templates, encoders, bounds, risk, preview, verification, and callback policy in reviewed project data. Reject unknown fields and duplicate ids before admitting any caller input."
claims_anchors = ["anchor/crate/sim-estate-project"]
supports = ["feature/sim-estate/estate-vocabulary"]

[[feature]]
id = "feature/sim-estate/model-provider-conformance"
title = "Deterministic estate provider conformance"
summary = "Exercise the shared provider suite against a clocked scripted model with every dispatch and verification fault."
owner = "crate/sim-site-estate-model"
audiences = ["code", "framework"]
guidance = "Use ModelEstate for lifecycle, race, cancellation, malformed-event, and reconciliation tests. It is host-free and must never acquire a process port."
claims_anchors = ["anchor/crate/sim-site-estate-model"]
supports = ["feature/sim-estate/estate-vocabulary"]

[[feature]]
id = "feature/sim-estate/sealed-command-site"
title = "Sealed command estate site"
summary = "Compose closed exposure operations with the canonical ProcessPort using only bound programs, roots, whole atoms, and explicit budgets."
owner = "crate/sim-site-estate-command"
audiences = ["code", "framework"]
guidance = "Use this adapter when a reviewed exposure maps directly to a sealed command. Fingerprint HEAD, tracked changes, relevant untracked inputs, effective configuration, and the exposure before approval."
claims_anchors = ["anchor/crate/sim-site-estate-command"]
supports = ["feature/sim-estate/exposure-authoring"]

[[feature]]
id = "feature/sim-estate/ansible-provider-site"
title = "Sealed Ansible estate provider"
summary = "Decorate ProcessPort with fixed inventory discovery, literal Make operations, an injected aggregate callback, and hash-chained private evidence."
owner = "crate/sim-site-estate-ansible"
audiences = ["code", "framework"]
guidance = "Use sanitized inventory identities and reconcile from the callback artifact after controller loss. Never decode human output or expose Ansible command, path, environment, hostvars, or result payloads."
claims_anchors = ["anchor/crate/sim-site-estate-ansible"]
supports = ["feature/sim-estate/sealed-command-site"]

[[feature]]
id = "feature/sim-estate/durable-operation-book"
title = "CAS-backed estate operation history"
summary = "Store immutable plans and event envelopes before atomically advancing linked run heads, with projections rebuilt solely from durable history."
owner = "crate/sim-lib-estate-book"
audiences = ["code", "framework"]
guidance = "Use Book over the platform Table/Dir implementation. Treat head conflicts and approval-use conflicts as observed refusals; never overwrite or infer an outcome from a missing event."
claims_anchors = ["anchor/crate/sim-lib-estate-book"]
supports = ["feature/sim-estate/estate-vocabulary"]

[[feature]]
id = "feature/sim-estate/guarded-operation-lifecycle"
title = "Guarded estate plan, apply, and reconcile"
summary = "Compose content-bound plans, reviewed single-use approvals, canonical leases, ledgered provider effects, typed verification, and durable quarantine."
owner = "crate/sim-lib-estate"
audiences = ["code", "framework"]
guidance = "Plan from one discovery, review the plan ContentKey, and apply through EffectGate. Reconcile reads the book and private artifacts but cannot perform; quarantine requires an explicit reviewed resolution."
claims_anchors = ["anchor/crate/sim-lib-estate"]
supports = ["feature/sim-estate/durable-operation-book", "feature/sim-estate/model-provider-conformance", "feature/sim-estate/ansible-provider-site"]

[[feature]]
id = "feature/sim-estate/composable-product-surface"
title = "One projected and callable estate"
summary = "One read-only Dir and one typed call vocabulary serve terminal, Surface, SDK, and capability-shaped automation without command injection."
owner = "crate/sim-lib-estate-serve"
audience = "framework"
guidance = "Read EstateDir rows for observation and submit Call values to the organ for effects. Automated callers may apply only an exact pre-approved plan and cannot issue review evidence."
claims_anchors = ["anchor/crate/sim-lib-estate-serve"]
supports = ["feature/sim-estate/guarded-operation-lifecycle"]

[[route]]
id = "route/author-estate-exposure"
task = "Author a safe estate exposure"
audiences = ["code", "framework"]
steps = [
  { feature = "feature/sim-estate/exposure-authoring", why = "The closed compiler admits only reviewed templates and shaped values." },
  { feature = "feature/sim-estate/model-provider-conformance", why = "The deterministic provider proves lifecycle behavior before physical placement." },
  { feature = "feature/sim-estate/sealed-command-site", why = "The command site preserves the closed declaration at the process boundary." },
  { feature = "feature/sim-estate/ansible-provider-site", why = "The Ansible decoration adds sanitized discovery and durable typed callback evidence." },
]

[[route]]
id = "route/plan-review-apply-estate-change"
task = "Plan, review, and apply an estate change"
audiences = ["code", "framework"]
steps = [
  { feature = "feature/sim-estate/exposure-authoring", why = "Compile a closed operation and shaped values." },
  { feature = "feature/sim-estate/guarded-operation-lifecycle", why = "Intern the exact plan, consume review once inside the gate, lease targets, and dispatch durably." },
  { feature = "feature/sim-estate/durable-operation-book", why = "Browse the linked event truth and rebuild projections." },
]

[[route]]
id = "route/reconcile-quarantined-estate-run"
task = "Reconcile or resolve a quarantined estate run"
audiences = ["code", "framework"]
steps = [
  { feature = "feature/sim-estate/durable-operation-book", why = "Read the existing run and retained evidence without dispatch." },
  { feature = "feature/sim-estate/guarded-operation-lifecycle", why = "Close, renew, release, or retain quarantine from observed artifacts; a mutation needs a new reviewed plan." },
  { feature = "feature/sim-estate/model-provider-conformance", why = "Prove unknown-after-dispatch and reviewed resolution before using a physical site." },
]

[[route]]
id = "route/expose-estate-product"
task = "Expose estate behavior through terminal, Surface, SDK, or agent tools"
audience = "application"
steps = [
  { feature = "feature/sim-estate/composable-product-surface", why = "Use the shared projection and exact capability-shaped calls." },
  { feature = "feature/sim-estate/guarded-operation-lifecycle", why = "All effects converge on the guarded organ and durable book." },
]

[enforcement.audience]
user = "strict"
framework = "strict"
code = "strict"
[enforcement.specimen]
user = "strict"
framework = "strict"
code = "strict"
[enforcement.route]
major_entrypoints = "strict"
frameworks = "strict"
