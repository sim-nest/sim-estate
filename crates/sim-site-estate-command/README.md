# sim-site-estate-command

Purely compiles validated estate operations into canonical sealed `ProcessRequest` values. Programs and roots are opaque manifest bindings; arguments are whole atoms and the child environment is empty by default.
