# sim-lib-estate

The estate operation organ: discover once, construct and intern an immutable
plan, consume reviewed approval inside the operation gate, lease exact targets,
persist before dispatch, and preserve uncertainty through reconciliation.
