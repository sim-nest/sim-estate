# sim-lib-estate-serve

Projects the durable estate book as one read-only directory and defines the exact typed calls shared by terminal, Surface, SDK, and agent-server adapters. Mutation is only possible by submitting a call to the estate organ; projection rows cannot execute behavior.
