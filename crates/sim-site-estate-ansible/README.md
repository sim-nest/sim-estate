# sim-site-estate-ansible

A sealed Ansible provider using exactly one fixed inventory request, literal Make targets, an injected aggregate callback, hash-chained private event artifacts, and separate capped human output.
