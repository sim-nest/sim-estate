# Reconcile after controller loss

Watch the provider-owned event artifact while `perform` blocks, kill the controller only after dispatch, then reconstruct the typed outcome from the surviving hash-chained artifact. Reconciliation performs no redispatch and never reads the separate human-output artifact.
